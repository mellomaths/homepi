server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name homepi.local;

    location / {
        return 200 'HomePi Dashboard - Access: glance.homepi.local, portainer.homepi.local';
        add_header Content-Type text/plain;
    }
}
