services:
  bot:
    container_name: lifesoundtrack-bot
    image: ${REGISTRY:-192.168.1.100:5000}/${IMAGE_NAME:-lifesoundtrack-bot}:${TAG:-latest}
    restart: unless-stopped
    pull_policy: always
